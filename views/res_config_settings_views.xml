<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Standalone settings form (kept independent of hr_attendance's own
         settings layout, so this module installs cleanly regardless of
         how that view is structured on your server). -->
    <record id="view_otm_attendance_report_settings_form" model="ir.ui.view">
        <field name="name">otm.attendance.report.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="Attendance Report Settings">
                <sheet>
                    <group string="Default Shift Timing (used when an employee has no individual shift set)">
                        <field name="otm_default_shift_start" widget="float_time"/>
                        <field name="otm_default_shift_end" widget="float_time"/>
                    </group>
                    <group string="Late Arrival">
                        <field name="otm_default_grace_minutes"/>
                    </group>
                    <group string="Overtime">
                        <field name="otm_default_overtime_threshold"/>
                    </group>
                </sheet>
                <footer>
                    <button string="Save" type="object" name="execute" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_otm_attendance_report_settings" model="ir.actions.act_window">
        <field name="name">Attendance Report Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_otm_attendance_report_settings_form"/>
        <field name="target">new</field>
    </record>
</odoo>
